<IfModule mod_rewrite.c>
  RewriteEngine on
  RewriteBase /
  
#  RewriteRule ^(.*)device/([^/]+)/(min/.*)$ $1$3?layout=$2 [QSA,L]
#  
#  RewriteCond %{REQUEST_URI} !^.*/min/.*$
#  RewriteRule ^(.*)device/([^/]+)/(.*)$ index.php?layout=$2&page=$1$3 [QSA,L]
#
#  RewriteCond %{REQUEST_URI} !^.*/min/.*$
  
  RewriteRule ^(.*)(device/[^/]+/)(min/.*)$ $1$3?pprefix=$2 [QSA,L]

  RewriteRule ^(.*)$ index.php?q=$1 [QSA,L]

</IfModule>
